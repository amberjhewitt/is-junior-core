@model IEnumerable<TeamsPractice.Models.Team>

@{
    ViewBag.Title = "ShowTeams";
}

<h2>ShowTeams</h2>

<p>
    @Html.ActionLink("Add a New Team", "AddTeam")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Team_Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Team_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditTeam", new { teamCode=item.Team_Code }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
